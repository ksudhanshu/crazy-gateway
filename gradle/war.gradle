apply plugin: "war"

bootWar {
    mainClassName = "com.appd.crazyevent.CrazyGatewayApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
